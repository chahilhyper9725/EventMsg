; PlatformIO Project Configuration File
[env:esp32dev]
platform = espressif32
board = esp32dev
framework = arduino

; Library dependency (assuming local development)
lib_deps = 
    ${PROJECT_DIR}/../../   ; Include the parent EventMsg library

monitor_speed = 115200      ; Match the Serial baud rate in the example
